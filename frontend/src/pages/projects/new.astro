---
/**
 * Create New Project Page - Protected Route
 * 
 * Access to this page is protected by server-side middleware.
 * Unauthenticated users are redirected to /login before this page renders.
 */
import { CreateProjectView } from '../../components/CreateProjectView';

// User is guaranteed to exist here - middleware redirects unauthenticated users
const { user } = Astro.locals;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Create a new building project with House Building Assistant" />
    <title>Create New Project - House Building Assistant</title>
  </head>
  <body>
    <CreateProjectView client:load />
  </body>
</html>
